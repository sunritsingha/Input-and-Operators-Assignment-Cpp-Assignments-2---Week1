#include<iostream>
using namespace std;
int main(){
    char a, b;
    cout << "enter the two characters: ";
    cin >> a >> b;
    cout << "the difference between the ASCII values is: " << (int)a - (int)b << endl;
}